import { describe, test, expect } from 'vitest';
import App from './App';

describe('App Scaffolding', () => {
  test('App component is defined and imports successfully', () => {
    expect(App).toBeDefined();
  });
});
